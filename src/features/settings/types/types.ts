import { StudentsSelectType } from "../../students/types/types"

export type ProfileInfo = {
    id: string
    name: string
    lastName: string
    email: string
    role: string
}

export type TutorChild = StudentsSelectType
