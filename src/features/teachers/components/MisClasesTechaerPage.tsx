import { FullSession } from "@/src/lib/auth-server"

type Props = {
  session: FullSession;
}

export default function MisClasesTechaerPage({ session }: Props) {
  return (
    <div>TeacherPageMisClases</div>
  )
}
